{ 
    "name": "{{ field.name }}"
  , "type" : "uint{{ field.data_type.bit_length }}"
  , "bit_length" : {{ field.data_type.bit_length }}
  , "cast_mode" : "{{ field.data_type.cast_mode.name }}"
{%- if field is constant %}
  , "const_value" : {{ field.value }}
{% endif -%}
}